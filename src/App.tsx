import React from 'react';

import { Button, Wrapper } from './styled';

export default function App(): JSX.Element {
    return (
        <Wrapper>
            <Button>PING</Button>
        </Wrapper>
    );
}
